version: '3.3'

services:
  backend:
    build: ../backend
    env_file: ../.env
    volumes:
      - ../backend/:/app/
  nginx:
    image: nginx:1.19.3
    volumes:
       - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
    depends_on:
      - backend
